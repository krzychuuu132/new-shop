import React from 'react';
import PropTypes from 'prop-types';
import { StyledLink } from './PrimaryLink.styles';

const PrimaryLink = (props) => {
  return <StyledLink {...props} />;
};

PrimaryLink.propTypes = {};

export default PrimaryLink;
